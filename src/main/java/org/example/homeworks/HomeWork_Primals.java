/*
package org.example.homeworks;

import org.example.oop.Person;

public class HomeWork_Primals {
    String s = "This is John. He is 27 years old.";
    String name = s.substring(8,12);
    int age = Integer.parseInt(s.substring(20,22));
    Person person = new Person(name,age);
        person.setAge(age);
        person.setName(name);
    print(person.getAge()+ " " +person.getName());
        System.out.println(person);
}
*/
